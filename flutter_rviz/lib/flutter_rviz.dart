library flutter_rviz;

export 'struct/occupancy_grid.dart';

/// A Calculator. -- Remove this once the library is ready.
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;
}
